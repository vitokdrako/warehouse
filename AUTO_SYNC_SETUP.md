# 🔄 Автоматична синхронізація з OpenCart

## Що це?
Автоматична синхронізація забезпечує постійну актуалізацію даних між OpenCart (інтернет-магазин) та RentalHub (система управління прокатом).

## Що синхронізується?

### 📦 Товари
- Назви, описи, SKU
- Ціни оренди
- Фотографії товарів
- Кількість на складі
- Атрибути (колір, розмір тощо)

### 📁 Категорії
- Структура категорій
- Назви та описи
- Порядок сортування

### 🛒 Замовлення
- Нові замовлення від клієнтів
- Інформація про клієнтів
- Склад замовлення
- Дати оренди

## Як це працює?

### Автоматична синхронізація
**Частота:** Кожні 30 хвилин

**Процес:**
1. Supervisor запускає скрипт `/app/backend/auto_sync.sh`
2. Скрипт виконує `/app/backend/sync_all.py`
3. Дані синхронізуються з OpenCart
4. Логи записуються в `/var/log/sync.log`

### Ручна синхронізація
**URL:** `http://localhost:3000/sync`

**Використання:**
1. Відкрити панель синхронізації
2. Натиснути "🔄 Запустити синхронізацію зараз"
3. Дочекатися завершення
4. Перевірити оновлені дані

## Технічні деталі

### Файли
```
/app/backend/sync_all.py           - Основний скрипт синхронізації
/app/backend/auto_sync.sh          - Shell wrapper для автозапуску
/app/backend/routes/sync.py        - API endpoints
/app/frontend/src/pages/SyncPanel.jsx - UI панель
/etc/supervisor/conf.d/sync.conf   - Конфігурація Supervisor
```

### API Endpoints
```
POST /api/sync/trigger       - Запустити синхронізацію
GET  /api/sync/status        - Отримати статус
GET  /api/sync/last-sync     - Інформація про останню синхронізацію
```

### Supervisor
```bash
# Перевірити статус
sudo supervisorctl status sync

# Перезапустити
sudo supervisorctl restart sync

# Зупинити/запустити
sudo supervisorctl stop sync
sudo supervisorctl start sync

# Переглянути логи
tail -f /var/log/supervisor/sync.out.log
tail -f /var/log/supervisor/sync.err.log
tail -f /var/log/sync.log
```

## База даних OpenCart

### Підключення
Параметри підключення до OpenCart зберігаються в `/app/backend/.env`:
```
OPENCART_DB_HOST=...
OPENCART_DB_USER=...
OPENCART_DB_PASSWORD=...
OPENCART_DB_NAME=...
```

### Таблиці
Синхронізація читає дані з:
- `oc_product` - товари
- `oc_category` - категорії
- `oc_order` - замовлення
- `oc_product_to_category` - зв'язки товарів і категорій
- `oc_product_attribute` - атрибути товарів

## Моніторинг

### Перевірка роботи
```bash
# Чи працює синхронізація?
sudo supervisorctl status sync

# Останні логи
tail -50 /var/log/sync.log

# Перевірити останню синхронізацію через API
curl http://localhost:8001/api/sync/last-sync | jq
```

### Типові проблеми

**Проблема:** Sync не запускається
```bash
# Рішення 1: Перевірити конфігурацію
sudo supervisorctl reread
sudo supervisorctl update

# Рішення 2: Перезапустити
sudo supervisorctl restart sync
```

**Проблема:** Помилка підключення до OpenCart
```bash
# Перевірити credentials в .env
cat /app/backend/.env | grep OPENCART

# Перевірити з'єднання
mysql -h $OPENCART_DB_HOST -u $OPENCART_DB_USER -p$OPENCART_DB_PASSWORD
```

**Проблема:** Товари не оновлюються
```bash
# Запустити повну синхронізацію вручну
cd /app/backend
python3 sync_all.py

# Або через API
curl -X POST http://localhost:8001/api/sync/trigger
```

## Продуктивність

### Час виконання
- **Категорії:** ~1-2 секунди
- **Товари (інкрементальна):** ~5-10 секунд
- **Товари (повна):** ~2-3 хвилини (6,000+ товарів)
- **Замовлення:** ~5-10 секунд

### Оптимізація
Синхронізація товарів відбувається інкрементально:
- Тільки оновлені товари (за `date_modified`)
- Використовує `LIMIT 1000` для пакетної обробки
- Пропускає товари без змін

## Безпека

### Логування
Всі дії синхронізації логуються з мітками часу:
```
[16:37:28] 📁 Syncing categories...
[16:37:51]   ✅ Synced 154 categories
[16:37:51] 📦 Syncing products (incremental)...
```

### Доступ
API синхронізації **не потребує аутентифікації** для локальних запитів.
Для production слід додати аутентифікацію в `routes/sync.py`.

## Розширення

### Додавання нових об'єктів для синхронізації
1. Додати функцію в `sync_all.py`:
   ```python
   def sync_new_entity():
       # Ваша логіка
   ```
2. Викликати в `main()`:
   ```python
   def main():
       sync_new_entity()
   ```

### Зміна частоти синхронізації
Відредагувати `/app/backend/auto_sync.sh`:
```bash
# Змінити sleep на потрібне значення (в секундах)
sleep 1800  # 30 хвилин
```

---

**Створено:** 25.11.2025  
**Версія:** 1.0  
**Статус:** ✅ Працює
