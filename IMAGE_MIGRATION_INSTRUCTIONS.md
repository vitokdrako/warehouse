# 🖼️ ІНСТРУКЦІЯ ПО МІГРАЦІЇ ЗОБРАЖЕНЬ З OPENCART

## Що робить скрипт?

Скрипт **`migrate_images_from_opencart.py`** автоматично:
1. 📥 Скачує оригінальні фото товарів з OpenCart (farforrent.com.ua)
2. 💾 Зберігає їх локально в RentalHub
3. 🔄 Створює 3 версії кожного фото:
   - **Оригінал** (повний розмір)
   - **Medium** (800x800px)
   - **Thumbnail** (300x300px)
4. 🗄️ Оновлює БД з новими шляхами до зображень

---

## Навіщо це потрібно?

**Проблема зараз:**
- Зображення товарів посилаються на OpenCart сервер
- Якщо OpenCart змінить структуру або видалить фото - вони зникнуть з RentalHub

**Після міграції:**
- ✅ Всі фото зберігаються локально в RentalHub
- ✅ Три розміри для оптимізації швидкості
- ✅ Повний контроль над зображеннями
- ✅ Незалежність від OpenCart

---

## Структура зберігання

```
/home/farforre/farforrent.com.ua/rentalhub/backend/uploads/products/
├── SKU123_1733075432.jpg              ← Оригінал
├── thumbnails/
│   └── SKU123_1733075432_thumb.jpg    ← 300x300px
└── medium/
    └── SKU123_1733075432_medium.jpg   ← 800x800px
```

БД зберігає: `uploads/products/SKU123_1733075432.jpg`

---

## Як запустити?

### 1️⃣ Тестовий режим (5 товарів)

```bash
cd /app/backend
python3 migrate_images_from_opencart.py --test
```

Це обробить тільки 5 товарів для перевірки.

---

### 2️⃣ Обробити перші 100 товарів

```bash
python3 migrate_images_from_opencart.py --limit 100
```

---

### 3️⃣ Обробити ВСІ товари

```bash
python3 migrate_images_from_opencart.py
```

⚠️ **Увага:** Це може зайняти багато часу якщо у вас тисячі товарів!

---

## Що відбувається під час міграції?

### Output приклад:

```
======================================================================
🖼️  МІГРАЦІЯ ЗОБРАЖЕНЬ З OPENCART → RENTALHUB
======================================================================
✅ Using PRODUCTION path: /home/farforre/.../uploads/products
📊 Fetching products from OpenCart...
📦 Found 1523 products with images

[1/1523] Product ID: 123
📥 Processing: SKU FI8685 - Тарілка десертна "Романтика"
  URL: https://www.farforrent.com.ua/image/cache/catalog/products/FI8685.jpg
  ✅ Saved original: FI8685_1733075432.jpg
  ✅ Created thumbnail: FI8685_1733075432_thumb.jpg
  ✅ Created medium: FI8685_1733075432_medium.jpg
  ✅ Updated DB: uploads/products/FI8685_1733075432.jpg

[2/1523] Product ID: 124
...

======================================================================
✅ МІГРАЦІЯ ЗАВЕРШЕНА
======================================================================
📊 Всього товарів:     1523
✅ Успішно:            1498
❌ Помилки:            25
⏱️  Тривалість:        342.5s (5.7 хв)
📈 Середній час/товар: 0.23s
======================================================================
```

---

## Особливості скрипта

### ✅ Переваги:

1. **Smart skip** - пропускає товари які вже мають локальні копії
2. **Retry механізм** - 3 спроби скачати кожне фото
3. **Batch commit** - оновлює БД після кожного товару
4. **Progress tracking** - показує прогрес у реальному часі
5. **Error handling** - продовжує роботу навіть якщо деякі фото не скачуються
6. **Image optimization** - стискає фото зберігаючи якість (quality=85)
7. **Format conversion** - конвертує RGBA/PNG в RGB/JPG автоматично

### ⚠️ Обмеження:

- Потребує доступу до інтернету (скачує з farforrent.com.ua)
- Займає місце на диску (приблизно 3-5MB на товар з трьома версіями)
- Може бути повільним для тисяч товарів

---

## Де зберігаються зображення?

### Production:
```
/home/farforre/farforrent.com.ua/rentalhub/backend/uploads/products/
```

### Local Dev:
```
/app/backend/uploads/products/
```

Скрипт автоматично визначає який шлях використовувати.

---

## Що робити після міграції?

### 1. Перевірити кілька товарів в UI

Зайдіть в RentalHub → Каталог → Перевірте чи відображаються фото

### 2. Перевірити БД

```sql
-- Скільки товарів мають локальні зображення
SELECT COUNT(*) 
FROM products 
WHERE image_url LIKE 'uploads/products/%';

-- Скільки ще посилаються на OpenCart
SELECT COUNT(*) 
FROM products 
WHERE image_url NOT LIKE 'uploads/products/%' 
  AND image_url IS NOT NULL;
```

### 3. Видалити старі посилання (ОПЦІОНАЛЬНО)

Якщо всі фото мігрували успішно, можна очистити старі посилання:

```sql
-- УВАГА: Запускайте тільки після перевірки!
UPDATE products 
SET image_url = NULL 
WHERE image_url NOT LIKE 'uploads/products/%'
  AND image_url IS NOT NULL;
```

---

## Troubleshooting

### ❌ Помилка: "Failed to download after 3 attempts"

**Причини:**
- Фото не існує на OpenCart сервері
- Неправильний шлях до фото
- Проблеми з інтернетом

**Рішення:**
- Скрипт автоматично пропустить такі фото
- Перевірте URL в логах і виправте вручну якщо потрібно

---

### ❌ Помилка: "Permission denied"

**Причина:** Немає прав на запис в директорію

**Рішення:**
```bash
sudo chmod -R 775 /home/farforre/farforrent.com.ua/rentalhub/backend/uploads/
sudo chown -R www-data:www-data /home/farforre/.../uploads/
```

---

### ❌ Помилка: "PIL.UnidentifiedImageError"

**Причина:** Пошкоджене або невалідне зображення

**Рішення:**
- Скрипт пропустить таке фото
- Перевірте оригінал на OpenCart сервері

---

## Запуск через cron (автоматична міграція)

Якщо хочете мігрувати нові товари автоматично:

```bash
# Кожного дня о 3:00 ночі мігрувати нові фото
0 3 * * * cd /home/farforre/farforrent.com.ua/rentalhub/backend && python3 migrate_images_from_opencart.py --limit 100 >> /var/log/image_migration.log 2>&1
```

---

## Перевірка після міграції

### Тест 1: Перевірити файли на диску

```bash
# Порахувати скільки фото
ls /home/farforre/.../uploads/products/ | wc -l
ls /home/farforre/.../uploads/products/thumbnails/ | wc -l
ls /home/farforre/.../uploads/products/medium/ | wc -l
```

Має бути однакова кількість у всіх трьох папках.

### Тест 2: Перевірити розміри

```bash
# Перевірити розмір оригіналів
du -sh /home/farforre/.../uploads/products/

# Перевірити що thumbnails менші
du -sh /home/farforre/.../uploads/products/thumbnails/
```

### Тест 3: API запит

```bash
# Перевірити що API повертає правильні шляхи
curl https://backrentalhub.farforrent.com.ua/api/public/products?limit=10
```

Повинні бути шляхи типу: `"image_url": "uploads/products/SKU_123.jpg"`

---

## FAQ

**Q: Чи можна запустити кілька разів?**  
A: Так! Скрипт пропускає товари які вже мають локальні копії.

**Q: Що якщо скрипт зупиниться посередині?**  
A: Можна просто запустити знову - він продовжить з того місця.

**Q: Чи видаляються старі фото з OpenCart?**  
A: Ні, скрипт тільки копіює. OpenCart фото залишаються.

**Q: Скільки місця потрібно?**  
A: Приблизно 3-5MB на товар × 3 версії = 9-15MB на товар

**Q: Як швидко це працює?**  
A: Приблизно 0.2-0.5 секунди на товар. 1000 товарів ≈ 5-10 хвилин.

---

## Підтримка

Якщо виникли проблеми:
1. Перевірте логи скрипта
2. Перевірте rights на папку uploads
3. Перевірте чи доступний OpenCart сервер
4. Перевірте чи правильно налаштована БД

---

**Дата створення:** 2025-12-01  
**Версія:** 1.0  
**Автор:** E1 AI Agent
