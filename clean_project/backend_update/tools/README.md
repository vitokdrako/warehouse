# Backend Tools / Інструменти Backend

Директорія містить допоміжні скрипти для обслуговування та міграції даних.

## 📁 Скрипти

### update_opencart.py
**Призначення:** Оновлення полів `price` та `ean` в базі даних OpenCart з Excel файлів.

**Використання:**
```bash
python3 /app/backend/tools/update_opencart.py
```

**Що робить:**
1. Читає Excel файли з директорії `/app/backend/`
2. Підключається до БД OpenCart (конфігурація з `.env`)
3. Оновлює поля `price` та `ean` для кожного `product_id`
4. Виводить статистику та перевіряє результати

**Залежності:**
- pandas
- openpyxl
- pymysql
- python-dotenv

**Конфігурація (.env):**
```
OC_DB_HOST=farforre.mysql.tools
OC_DB_PORT=3306
OC_DB_DATABASE=farforre_db
OC_DB_USERNAME=farforre_db
OC_DB_PASSWORD=***
OC_DB_PREFIX=oc_
```

**Безпека:**
- Скрипт запитує підтвердження перед оновленням
- Перевіряє існування кожного product_id
- Використовує параметризовані SQL запити
- Виконує commit тільки після успішного оновлення всіх записів

**Звіт:**
Після виконання дивіться файл `UPDATE_REPORT.md` для деталей.

---

## 🔧 Додавання нових інструментів

При додаванні нових скриптів в цю директорію, будь ласка:
1. Додайте shebang: `#!/usr/bin/env python3`
2. Додайте docstring з описом
3. Оновіть цей README
4. Додайте необхідні залежності в requirements.txt
