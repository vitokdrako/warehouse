# 📋 Order Workflow - Повний цикл замовлення

## Статуси замовлення

```
pending → awaiting_customer → processing → ready_for_issue → issued → on_rent → returned/partial_return → completed
                                                                           ↓
                                                                      cancelled (можливо з будь-якого статусу до issued)
```

### Детальний опис статусів:

| Статус | Опис | UI |
|--------|------|-----|
| `pending` | Нове замовлення (внутрішнє) | Новий ордер |
| `awaiting_customer` | Очікує підтвердження клієнта | Очікує клієнта |
| `processing` | Клієнт підтвердив, в обробці | На комплектації |
| `ready_for_issue` | Скомплектовано, готово до видачі | Готово до видачі |
| `issued` | Видано клієнту | Видано |
| `on_rent` | В оренді (активна оренда) | В оренді |
| `partial_return` | Часткове повернення (є товари в продовженні) | Часткове повернення |
| `returned` | Повернуто повністю | Повернено |
| `completed` | Завершено, архівоване | Завершено |
| `cancelled` | Скасовано | Скасовано |

---

## 1️⃣ Створення замовлення

**Endpoint:** `POST /api/orders`

**Початковий статус:** `awaiting_customer`

**Що відбувається:**
- Створюється запис в `orders`
- Генерується `order_number` (ORD-0001)
- Товари резервуються (фіксуються в `order_items`)
- Записується в `order_lifecycle`

---

## 2️⃣ Підтвердження клієнтом

**Endpoint:** `POST /api/decor-orders/{id}/confirm-by-client`

**Перехід:** `awaiting_customer` → `processing`

**Що відбувається:**
- Встановлюється `client_confirmed = TRUE`
- Статус змінюється на `processing`
- Записується в `order_lifecycle`

---

## 3️⃣ Комплектація (Issue Card)

**UI:** `/manager/issue-card/{id}` - `IssueCardWorkspace.jsx`

**Таблиця:** `issue_cards`

**Функціонал:**
- Перегляд та редагування позицій
- Чеклист комплектації (стрейчування, маркування, фото)
- Призначення комплектувальників
- Генерація документів (накладна, акт)

**Endpoint збереження:** `PUT /api/issue-cards/{id}`

---

## 4️⃣ Видача клієнту

**Перехід:** `processing` → `ready_for_issue` → `issued`

**Endpoint:** `PUT /api/orders/{id}/status`

**Що відбувається:**
- Статус змінюється на `issued`
- Фіксується `issued_at`, `issued_by`
- Товари переходять в статус "видано"
- Записується в `order_lifecycle`

---

## 5️⃣ В оренді

**Статус:** `on_rent` (опціонально, якщо потрібно відслідковувати)

Зазвичай використовується `issued` як активний статус оренди.

---

## 6️⃣ Повернення

**UI:** `/manager/return/{id}` - `ReturnOrderWorkspace.jsx`

### 6.1 Повне повернення

**Endpoint:** `POST /api/decor-orders/{id}/complete-return`

**Перехід:** `issued` → `returned`

**Що відбувається:**
- Перевіряються всі товари
- Фіксуються пошкодження (якщо є)
- Товари повертаються на склад
- Розраховуються штрафи (late_fee, damage_fee)
- Записується в `fin_payments`
- Статус → `returned`

### 6.2 Часткове повернення (НОВА СИСТЕМА ВЕРСІЙ)

**Замість "заморожування" товарів - створюємо версії замовлення!**

**UI:** Колонка "⚠️ Часткове повернення" на Dashboard

**Воркфлоу:**

1. **При частковому поверненні:**
   - Повернуті товари → назад на склад (доступні для оренди)
   - Неповернені товари → створюється версія `#OC-7280(1)`
   - Батьківське замовлення → статус `returned`

2. **Версія має:**
   - `order_number`: OC-7280(1), OC-7280(2), ...
   - `original_return_date`: дата коли мало бути повернуто
   - `daily_fee`: сума прострочення за день
   - `days_overdue`: автоматично розраховується
   - `fee_status`: pending → charged/waived

3. **Прийняття товарів з версії:**
   - Якщо все повернуто → версія закривається
   - Якщо не все → можна створити наступну версію `(2)`

4. **Нарахування прострочення:**
   - Система показує: "Розрахунок: 5 днів × ₴150 = ₴750"
   - Менеджер вводить суму вручну (може бути знижка)
   - Записується в `fin_payments` тип `late`

**Таблиці:**
- `order_return_versions` - основна інформація версії
- `order_return_version_items` - товари версії

**API:**
```
GET  /api/return-versions                      - Список активних версій
GET  /api/return-versions/{id}                 - Деталі версії
POST /api/return-versions/order/{id}/create    - Створити версію
POST /api/return-versions/{id}/accept-items    - Прийняти товари
POST /api/return-versions/{id}/create-next     - Створити наступну версію
POST /api/return-versions/{id}/charge-fee      - Нарахувати прострочення
POST /api/return-versions/{id}/waive-fee       - Списати прострочення
GET  /api/return-versions/order/{id}/versions  - Всі версії замовлення
POST /api/return-versions/migrate-from-extensions - Міграція старих даних
```

---

## 7️⃣ Прийняття товарів з продовження

**UI:** Той самий `ReturnOrderWorkspace.jsx` при статусі `partial_return`

**Endpoint:** `POST /api/partial-returns/order/{id}/accept-from-extension`

**Що відбувається:**
1. Для кожного товару:
   - Знаходить активне продовження (`order_extensions`)
   - Розраховує дні прострочення (від `original_end_date` до сьогодні)
   - Створює `fin_payments` з типом `late`
   - Закриває продовження (`status = 'completed'`)
   - Повертає товар на склад

2. Якщо всі товари повернуто:
   - Статус → `returned`
   - Записується в `order_lifecycle`

---

## 8️⃣ Завершення (Archive)

**Endpoint:** `POST /api/orders/{id}/archive`

**Перехід:** `returned` → `completed` (або архівація)

**Що відбувається:**
- `is_archived = 1`
- Замовлення приховується з активних

---

## 📊 Ключові таблиці

| Таблиця | Призначення |
|---------|-------------|
| `orders` | Основна інформація про замовлення |
| `order_items` | Товари в замовленні |
| `issue_cards` | Картки комплектації |
| `order_lifecycle` | Історія змін статусу |
| `order_extensions` | Продовження оренди (часткове повернення) |
| `partial_return_log` | Лог часткових повернень |
| `fin_payments` | Фінансові транзакції |
| `product_damage_history` | Історія пошкоджень |

---

## 🔄 API Endpoints Summary

### Orders
- `GET /api/orders` - Список замовлень
- `GET /api/orders/{id}` - Деталі замовлення
- `POST /api/orders` - Створити замовлення
- `PUT /api/orders/{id}` - Оновити замовлення
- `PUT /api/orders/{id}/status` - Змінити статус
- `DELETE /api/orders/{id}` - Видалити/скасувати

### Issue Cards
- `GET /api/issue-cards/{id}` - Картка комплектації
- `PUT /api/issue-cards/{id}` - Зберегти картку

### Returns
- `POST /api/decor-orders/{id}/complete-return` - Завершити повернення
- `PUT /api/decor-orders/{id}/return-progress` - Зберегти прогрес

### Partial Returns
- `GET /api/partial-returns/order/{id}/not-returned` - Неповернені товари
- `POST /api/partial-returns/order/{id}/process` - Обробити часткове
- `GET /api/partial-returns/order/{id}/extensions` - Продовження оренди
- `POST /api/partial-returns/order/{id}/accept-from-extension` - Прийняти з продовження
- `GET /api/partial-returns/order/{id}/extension-summary` - Підсумок продовжень

---

## 💰 Фінансові типи (fin_payments.payment_type)

| Тип | Опис |
|-----|------|
| `rent` | Оренда |
| `deposit` | Застава |
| `discount` | Знижка |
| `late` | Прострочення |
| `damage` | Пошкодження |
| `loss` | Повна втрата |
| `prepayment` | Передоплата |
| `refund` | Повернення коштів |

---

## 📱 Frontend Routes

| Route | Компонент | Призначення |
|-------|-----------|-------------|
| `/manager/orders/{id}` | `NewOrderViewWorkspace` | Перегляд/редагування |
| `/manager/issue-card/{id}` | `IssueCardWorkspace` | Комплектація |
| `/manager/return/{id}` | `ReturnOrderWorkspace` | Повернення |
| `/manager/finance` | `FinanceHub` | Фінансовий кабінет |
