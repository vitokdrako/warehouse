# RentalHub Deployment v2 - Changelog

## Зміни в цій версії:

### 1. Sync скрипт (`sync_production.py`)
- ✅ Додано синхронізацію SKU (артикулів) з OpenCart
- ✅ `color` та `material` більше НЕ перезаписуються при синхронізації

### 2. Фото пошкоджень (`routes/product_damage_history.py`)
- ✅ Виправлено шлях збереження фото на production

### 3. Новий роут (`routes/uploads.py`)
- ✅ API для віддачі завантажених файлів через `/api/uploads/`

### 4. Генерація документів (`services/doc_engine/data_builders.py`)
- ✅ Виправлено шляхи до фото для production

### 5. Видалення товарів із замовлення (`routes/order_modifications.py`)
- ✅ При видаленні товару - він автоматично повертається в наявність
- ✅ При відновленні товару - перевіряється наявність на складі

### 6. Виправлено помилку `api is not defined` (`IssueCardWorkspace.jsx`)

### 7. НОВЕ: Швидкий збір СЕТІВ з каталогу (`CatalogBoard.jsx`)
- ✅ Кнопка "🎁 Зібрати сет" - для комплектів оренди (10 тарілок + свічники + скатертина)
- ✅ Режим мультивибору - чекбокси на картках товарів
- ✅ Плаваюча панель внизу з вибраними товарами
- ✅ Модалка створення сету з кількістю кожного товару та ціною зі знижкою
- ✅ Після створення - перехід на вкладку "Сети"

### 8. НОВЕ: Швидкий збір НАБОРІВ з каталогу (`CatalogBoard.jsx`)
- ✅ Кнопка "📏 Зібрати набір" - для розмірної сітки (Свічник 17см, 20см, 25см)
- ✅ Окремий режим вибору з іншим кольором (жовтий/amber)
- ✅ Модалка створення набору (family) з назвою та описом
- ✅ Після створення - перехід на вкладку "Набори"

## Різниця між Сетами та Наборами:

| | Сети (Sets) | Набори (Families) |
|---|---|---|
| Призначення | Комплект для оренди разом | Групування схожих товарів |
| Приклад | 10 тарілок + 2 свічника + скатертина | Свічник 17см, 20см, 25см |
| Ціна | Можна встановити зі знижкою | Немає спільної ціни |
| Кількість | Кожен товар має свою кількість | По 1 кожного |

## Інструкція оновлення:

1. Скопіюйте файли `backend/` в `/home/farforre/farforrent.com.ua/rentalhub/backend/`
2. Скопіюйте файли `frontend_build/` в `/home/farforre/farforrent.com.ua/rentalhub/frontend/build/`
3. Перезапустіть backend: `sudo systemctl restart rentalhub-backend`
