# Changelog v2 - 20 січня 2026

## 🔧 Виправлення фінансової синхронізації

### Проблема
При редагуванні замовлення (зміна товарів, кількості днів) фінансові дані (`total_price`, `deposit_amount`) не оновлювались в таблиці `orders`, що призводило до неправильних сум на дашборді та у фінансовій консолі.

### Виправлення

#### 1. PUT /api/decor-orders/{id}/items
**Файл:** `backend/routes/orders.py`

Тепер при оновленні товарів автоматично перераховуються:
- `total_price` = сума (price × qty × rental_days)
- `deposit_amount` = сума (deposit × qty)
- `total_loss_value` = deposit_amount

#### 2. PUT /api/decor-orders/{id}
**Файл:** `backend/routes/orders.py`

Додано поля до `field_mapping`:
- `total_price`
- `deposit_amount`
- `total_loss_value`

Тепер при збереженні замовлення (кнопка "Зберегти") ці поля правильно записуються в БД.

---

## 📍 Покращення відображення локації товарів

### Зміни
У режимах комплектації, видачі та повернення тепер показується:
- **Зона** (zone)
- **Полиця** (aisle/shelf)

Формат: `📍 Зона: A • Полиця: 7`

### Оновлені файли:
- `frontend/src/components/order-workspace/zones/ZoneItemsPickup.jsx`
- `frontend/src/components/order-workspace/zones/ZoneItemsReturn.jsx`
- `frontend/src/components/order-workspace/zones/ZoneItemsList.jsx`

### Виправлено:
- Фільтрація значень `'None'` і `'null'` які приходили з бекенду
- Коректне відображення на мобільних пристроях

---

## 📦 Файли для деплою

```
deployment_v2/
├── backend/
│   ├── routes/
│   │   ├── orders.py         # Головні виправлення
│   │   └── ... (інші роути)
│   └── ...
└── frontend_build/
    ├── index.html
    └── static/
```

## ⚠️ Важливо
Після деплою перевірте:
1. Редагування товарів в замовленні → суми оновлюються
2. Зміна кількості днів → суми оновлюються
3. Локація товарів відображається (Зона + Полиця)
