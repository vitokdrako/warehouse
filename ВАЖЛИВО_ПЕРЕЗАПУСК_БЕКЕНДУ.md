# ⚠️ ВАЖЛИВО: Перезапуск бекенду після змін

## Проблема
Після внесення змін у Python код (`/app/backend/**/*.py`), бекенд НЕ перезапускається автоматично!

## Симптоми
- Ви змінили код
- Фронтенд працює з новими змінами (hot reload працює)
- Але API повертає старі результати або старі типи даних

## Рішення
Після зміни Python коду **ЗАВЖДИ** перезапускайте бекенд:

```bash
sudo supervisorctl restart backend
```

Перевірте що працює:
```bash
curl http://localhost:8001/api/manager/finance/ledger
```

## Що потребує перезапуску?
✅ Зміни в `/app/backend/**/*.py`
✅ Зміни в `/app/backend/requirements.txt`
✅ Зміни в `/app/backend/.env`

## Що НЕ потребує перезапуску?
❌ Зміни в `/app/frontend/**/*` (hot reload)
❌ Зміни в документації (`.md` файли)

## Приклад з цієї сесії
Ми змінили `orders.py` щоб використовувати `deposit_expected` замість `deposit_hold`, але забули перезапустити бекенд. Нове замовлення #6956 створилось зі старим типом. Після перезапуску + міграції даних все запрацювало.

---

**Запам'ятайте**: Python != JavaScript. Python потребує перезапуску! 🔄
