═══════════════════════════════════════════════════════════════
  RENTAL HUB - ВЕРСІЯ 20 - ШВИДКИЙ СТАРТ
  Deployment для: rentalhub.farforrent.com.ua
═══════════════════════════════════════════════════════════════

🌐 ВАШІ СЕРВЕРИ:

Frontend: https://rentalhub.farforrent.com.ua
Backend:  https://backrentalhub.farforrent.com.ua

═══════════════════════════════════════════════════════════════

📋 КРОК 1: ЗАВАНТАЖИТИ ФАЙЛИ

1. Завантажте всю папку /версія_20_deployment/ на ваш сервер
2. Розпакуйте у потрібну директорію

═══════════════════════════════════════════════════════════════

🔧 КРОК 2: BACKEND SETUP

1. Перейдіть до папки backend_files/:
   cd /path/to/версія_20_deployment/backend_files

2. Встановіть Python залежності:
   pip install -r requirements.txt

3. Налаштуйте .env файл:
   nano .env
   
   Перевірте/додайте:
   DB_HOST=localhost
   DB_USER=your_db_user
   DB_PASSWORD=your_db_password
   DB_NAME=rental_hub
   MONGO_URL=mongodb://localhost:27017/
   JWT_SECRET=your-secret-key-here

4. Запустіть міграції БД (якщо потрібно):
   # Виконайте SQL скрипти з папки migrations/
   
5. Запустіть backend:
   uvicorn server:app --host 0.0.0.0 --port 8001
   
   Або через systemd service (рекомендовано):
   sudo systemctl start rentalhub-backend

═══════════════════════════════════════════════════════════════

🎨 КРОК 3: FRONTEND SETUP

1. Frontend build вже готовий у папці build/

2. Налаштуйте Nginx:
   
   Для frontend (rentalhub.farforrent.com.ua):
   ─────────────────────────────────────────────────────
   server {
       listen 80;
       server_name rentalhub.farforrent.com.ua;
       
       root /path/to/версія_20_deployment/build;
       index index.html;
       
       location / {
           try_files $uri $uri/ /index.html;
       }
       
       # Кешування статики
       location /static/ {
           expires 1y;
           add_header Cache-Control "public, immutable";
       }
   }
   ─────────────────────────────────────────────────────
   
   Для backend (backrentalhub.farforrent.com.ua):
   ─────────────────────────────────────────────────────
   server {
       listen 80;
       server_name backrentalhub.farforrent.com.ua;
       
       location / {
           proxy_pass http://localhost:8001;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
       }
   }
   ─────────────────────────────────────────────────────

3. Перезапустіть Nginx:
   sudo nginx -t
   sudo systemctl reload nginx

4. Налаштуйте SSL (Let's Encrypt):
   sudo certbot --nginx -d rentalhub.farforrent.com.ua
   sudo certbot --nginx -d backrentalhub.farforrent.com.ua

═══════════════════════════════════════════════════════════════

🗄️ КРОК 4: БАЗА ДАНИХ

ВАЖЛИВО! Перед оновленням зробіть backup:
   mysqldump -u root -p rental_hub > backup_before_v20.sql

Виконайте міграції з папки backend_files/migrations/:
   
   1. add_user_tracking.sql
      - Додає поля created_by_id, received_by_id, checked_by_id
   
   2. create_product_damage_history.sql
      - Оновлює структуру damage_items та damages

Або виконайте все разом:
   cat migrations/*.sql | mysql -u root -p rental_hub

═══════════════════════════════════════════════════════════════

✅ КРОК 5: ПЕРЕВІРКА

1. Перевірте backend:
   curl https://backrentalhub.farforrent.com.ua/api/health
   
   Має повернути: {"status": "ok"}

2. Відкрийте у браузері:
   https://rentalhub.farforrent.com.ua
   
3. Увійдіть і перевірте:
   - Кабінет шкоди → Має бути вкладки (Головна, Мийка, Реставрація, Хімчистка)
   - Dashboard → Має бути нова шапка з логотипом RH
   - Прийміть повернення з брудними товарами → Має автоматично створитись damage case

═══════════════════════════════════════════════════════════════

🔐 SYSTEMD SERVICE (рекомендовано)

Створіть файл: /etc/systemd/system/rentalhub-backend.service

─────────────────────────────────────────────────────
[Unit]
Description=Rental Hub Backend API
After=network.target mysql.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/версія_20_deployment/backend_files
Environment="PATH=/usr/bin:/usr/local/bin"
ExecStart=/usr/bin/uvicorn server:app --host 0.0.0.0 --port 8001
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
─────────────────────────────────────────────────────

Запустіть:
   sudo systemctl daemon-reload
   sudo systemctl enable rentalhub-backend
   sudo systemctl start rentalhub-backend
   sudo systemctl status rentalhub-backend

═══════════════════════════════════════════════════════════════

🐛 TROUBLESHOOTING

1. Backend не стартує:
   - Перевірте логи: journalctl -u rentalhub-backend -f
   - Перевірте .env файл
   - Перевірте підключення до БД

2. Frontend показує помилку:
   - Перевірте Nginx конфіг
   - Перевірте CORS на backend
   - Перевірте browser console (F12)

3. API повертає 500:
   - Перевірте backend логи
   - Перевірте міграції БД
   - Перевірте підключення до БД

4. Damage cases не створюються:
   - Перевірте що таблиця damages існує
   - Перевірте що таблиця damage_items існує
   - Перевірте логи backend

═══════════════════════════════════════════════════════════════

📞 ПІДТРИМКА

Email: support@rentalhub.ua
Документація: /версія_20_deployment/ЗМІНИ_ВЕРСІЯ_20.txt

═══════════════════════════════════════════════════════════════

Успішного розгортання! 🚀
